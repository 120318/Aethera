[pytest]
testpaths =
    tests
markers =
    drift: state drift and convergence tests
    health: state drift and convergence tests
    aggregation: repository sample service filtering and sorting
filterwarnings =
    error
    ignore:Exception ignored in:pytest.PytestUnraisableExceptionWarning
